page 70 
# Configurer Git
git config
    ## avoir la liste des configurations
    git config --list

# initialiser un projet git
git init

# Telecharger un projet git d'un repo
git clone [url]

# avoir le status du projet
git status
git diff

# Track ou stage un fichier
git add

# Ecrire un commit sur les fichier stager modfier
git commit

# Avoir l'historique des commits
git log

### REMOTE
# Avor la liste des remotes
git remote -v
git remote show [remote-name] #plus d'infos
    ## Add remote
    git remote add <shortname> <url>
